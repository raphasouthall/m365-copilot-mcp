{
  "name": "m365-copilot-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server that drives the authenticated M365 Copilot web UI on the work laptop via a paired Edge extension, so Claude Code can ask Copilot questions.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.2",
    "ws": "^8.18.0",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=18.18"
  }
}
